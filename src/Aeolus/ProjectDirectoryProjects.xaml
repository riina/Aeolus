<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:localmodelproxies="using:Aeolus.ModelProxies"
             xmlns:local="clr-namespace:Aeolus"
             x:Class="Aeolus.ProjectDirectoryProjects"
             Title="Projects">

    <ScrollView>
        <VerticalStackLayout
            Spacing="10"
            Padding="10,0"
            VerticalOptions="StartAndExpand">
            <HorizontalStackLayout Spacing="10">
                <Button Style="{DynamicResource smolBtnStyle}"
                x:Name="RefreshBtn"
                Text="Refresh"
                SemanticProperties.Hint="Refresh project list"
                Clicked="RefreshBtn_Clicked"
                HorizontalOptions="Start" />
                <ActivityIndicator BindingContext="{x:Static local:App.Me}" IsRunning="{Binding Busy}" />
            </HorizontalStackLayout>

            <CollectionView x:Name="projectList" x:DataType="localmodelproxies:ProjectDirectoryProject">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <local:ProjectDirectoryProjectEntry />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
